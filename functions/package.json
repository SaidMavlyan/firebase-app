{
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json src/**/*.ts -e tests/**/*.ts",
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "mocha -r ts-node/register tests/**/*.spec.ts --watch"
  },
  "engines": {
    "node": "8"
  },
  "main": "lib/index.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "firebase-admin": "^8.6.0",
    "firebase-functions": "^3.3.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "@types/mocha": "^7.0.2",
    "firebase-functions-test": "^0.1.7",
    "mocha": "^7.1.1",
    "ts-node": "^8.8.1",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  },
  "private": true
}
